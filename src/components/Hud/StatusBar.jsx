import React from "react";
import styled from "styled-components";

const Container = styled.div`
  flex: 1;
  background-color: green;
`;

const StatusBar = () => <Container>STATUS_BAR</Container>;

export default StatusBar;
